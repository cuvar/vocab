import "../globals.css";
import PlausibleProvider from "next-plausible";

export default function App({ Component, pageProps }) {
  return (
    <PlausibleProvider domain="vocab-docs.vercel.app">
      <Component {...pageProps} />
    </PlausibleProvider>
  );

}
